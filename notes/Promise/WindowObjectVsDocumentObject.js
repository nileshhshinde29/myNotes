/* 

1. `window` Object:
        The `window` object is the global object in a web browser environment. It represents the browser window and 
        provides access to various properties and methods related to the browser window.
        The `window` object is available in both the global scope and any code running inside the web page.

Some common properties and methods of the `window` object include:

- `window.document`: A reference to the `document` object, representing the current HTML document loaded in the window.
- `window.alert()`:
- `window.setTimeout()`
- `window.location`: Provides information about the current URL and allows navigation to a new URL.
- `window.localStorage`: Provides access to the browser's local storage, allowing data to be stored and retrieved.

2. `document` Object:
        The `document` object is a property of the `window` object, and it represents the DOM (Document Object Model) of the current web page.
        It provides access to the HTML elements and their content within the current page.
        The `document` object is used to interact with and manipulate the content of the web page.

Some common properties and methods of the `document` object include:

- `document.getElementById()`: 
- `document.querySelector()`: 
- `document.createElement()`: Creates a new HTML element in memory, which can be added to the DOM later.
- `document.body`: A reference to the `<body>` element of the current page.
- `document.title`: The title of the current page.

*/